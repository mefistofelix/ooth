

## Engineering preferences

These principles apply across languages, runtimes, and frameworks.

### Simplicity and readability

- Optimize for cognitive simplicity first and line count second.
- Keep code minimal and readable. Do not use code golf or compress unrelated operations onto one line.
- Let each line express one clear idea.
- Prefer guard clauses for empty, error, and exceptional cases so the main path stays flat.
- Prefer structured data transformations over clever string manipulation.
- Use descriptive names. Do not introduce short aliases solely to reduce repetition.

### Native constructs and dependencies

- Prefer direct language, runtime, and standard-library features over wrappers or custom replacements.
- Do not introduce third-party dependencies without explicit approval.
- Rely on native validation, errors, and defaults instead of reimplementing them.
- Keep the origin of an operation visible when qualification improves clarity. Avoid redundant qualification when the imported symbol already communicates its origin.
- Do not hide ordinary public fields or attributes behind trivial accessors or properties.
- In languages with type inference, avoid redundant annotations. Keep types where required or where they clarify a public boundary.

### Abstractions and boundaries

- Add helpers, abstractions, validation layers, comments, or documentation only when they solve a concrete problem.
- Prefer a few obvious duplicated lines over an abstraction that hides semantics.
- Keep abstraction boundaries strict: lower layers expose generic representations; higher layers translate domain- or protocol-specific concepts.
- Generic parsers and formatters must not branch on concrete drivers unless that syntax belongs to the generic layer.
- At protocol boundaries, prefer a structured argument when a low-level function signature would otherwise keep growing.
- Assume established internal data contracts. Do not add machinery for every theoretical malformed input.

### Errors and concurrency

- Handle errors when they affect program logic or when additional context is actionable. Otherwise, let native errors propagate.
- Do not add broad catches, defensive checks, or fallback behavior merely to suppress possible errors.
- Do not wrap synchronous or blocking APIs in asynchronous functions unless real concurrency is required.
- When blocking work must coexist with asynchronous code, make the boundary explicit at the caller using the language's standard thread or executor mechanism.

## ooth project decisions and state

- The user requested the highest feasible common lifecycle for all worker runtimes and WebSocket tests, plus a dedicated generic-worker protocol section in README and a technical findings ledger. `TestRuntimeLifecycle` now runs real ooth cold/hot activation, mean-occupancy growth to two ready workers, both serving PIDs, balanced events, idle zero, reactivation and active-response draining: direct/Caddy/Nginx, Linux and Windows amd64. Python standard sample is HTTP/1.1; Node/Bun/Deno are HTTP/1.1+h2c; Socketify is HTTP/1.1; Windows TrueAsync is HTTP/1.1+h2c. JS uses shared node_worker.cjs with existing adapters; Socketify uses TEST_OOTH_PROTOCOL=1; TrueAsync uses worker.php. No core change, new application dependency or toolchain patch. Official Linux TrueAsync 0.10.0 is static without FFI; the user explicitly declined runtime recompilation. Record six skipped native-server cases, not passing coverage; its prior async-accept probe remains valid.
- Final local matrix on 2026-09-24 passed 30 Windows and 24 Linux cases, with six Linux TrueAsync skips; portable per-case results are in tools/probes/runtime/results.csv. Full native regression tests (excluding that already-run matrix) and go vet passed with CGO_ENABLED=0 on both OSes, including original ExtraHandle probes, Node lifecycle and Caddy/Nginx/PHP-CGI. Pressure research was not rerun; different-account Windows and root-required Linux identity tests remain separately gated.
- HTTP/1.1 cases for Node/Bun/Deno/Socketify/Windows TrueAsync test RFC6455 upgrade, text/binary echo, ping/pong, survival beyond idle TTL, client normal close and server 1001 close on stdin stop, with another HTTP response in flight. One request event pair spans each WS session. Socketify/TrueAsync use native WS engines; JavaScript uses a bounded test-only upgrade echo (masked, unfragmented frames <=125 bytes), not a general WS library or native Bun.serve/Deno.upgradeWebSocket certification. No RFC8441, TLS or compression coverage. Core protocol remains ready/start/end on stdout and handshake-gated stop on stdin.
- `tools/probes/runtime/FINDINGS.md` preserves observed balancing, adoption and teardown behavior and separates verified mechanisms from unresolved causes. Caddy can pin h2c streams to one worker; the new lifecycle fixture explicitly disables upstream h2c keepalive, while the earlier pressure fixture retains sessions. Shared accept is atomic but does not promise round-robin fairness. Socketify's growth workload blocks the first loop for 600 ms; probe new capacity while that workload remains active, not only after its responses. Three Windows Caddy repetitions passed after that harness correction. Do not claim a kernel fairness fix or performance gains. Peer ECONNRESET/EPIPE are handled per connection; Deno injected HTTP/1 closes after response; JS adapter exit defers close events/stdout flush; Socketify drains before stopping its loop; TrueAsync WS uses explicit recv (the earlier iterator stall's cause is not isolated). Sources/config templates/tests are versioned, generated logs/configs live under ignored build/ via OOTH_TEST_ARTIFACTS. No GitHub build dispatch.

- The user explicitly changed the worker handoff default to an extra inherited listener on BOTH Linux and Windows, advertised as decimal OOTH_LISTEN_HANDLE. App socket_handoff is env/omitted (default), stdin or numeric 0 (legacy), or an exact Linux fd 3..1024. Reject stdout/stderr and arbitrary fd selection on Windows; native SOCKET values are not CRT fd slots. Linux uses ExtraFiles, Windows AdditionalInheritedHandles, preserving identity and atomic Job/cgroup placement; no new toolchain patch or application dependency. Stale parent OOTH_LISTEN_HANDLE is removed. Existing stdin workers must opt into socket_handoff: stdin. Stock PHP-CGI fixtures do so explicitly.
- The user explicitly authorized graceful commands on the now-free stdin pipe, superseding the earlier signals-only decision. main.go sends one `v=1 event=stop ts=<Unix-ns>` line only after that worker's valid stdout ready handshake. No simultaneous signal after a successful write. Without handshake, with legacy stdin listener, or on write failure, use existing OS notification. stop_timeout still forces whole-family termination. Virtual services can use the same handshake/stop. No change to Process.Signal or toolchain behavior. Node/Python examples and Go fixtures implement it. Node explicitly destroys the stdin pipe when its readline interface closes; readline.close alone kept Windows workers alive until forced timeout. TestNodeWorker now rejects any graceful-timeout kill, as well as checking complete responses and graceful markers. TestWorkerStopTransport distinguishes command from signal, checks ordinary-output gating, init services, legacy stdin, ignored command/force kill, and Linux fd 7; TestSocketHandoffConfig covers numeric YAML and invalid/platform-specific values.
- TestJavaScriptExtraHandle and tools/probes/javascript save Node 26.10.0, Bun 1.4.2 and Deno 2.9.7 positive Windows/Linux amd64 experiments: three workers share one extra listener, each serves a full HTTP/1.1 body, ordinary stdin works, sequential stdin stops preserve survivors, and the final pending response drains. Node uses public fd on Linux/private TCPWrap on Windows. Bun Linux uses net.Server.listen({fd}) feeding its built-in node:http connection handler; Windows uses bun:ffi WSAPoll/accept in a worker thread and internal fdIsRawSocket connected-socket adoption. The direct Windows listener path still intermittently stalls stdin stop even with FIONBIO; root cause unisolated, TEST_BUN_DIRECT preserves reproduction; native Bun.serve/http.Server.listen does not adopt fd. Deno Linux uses node:http fd adoption; Windows requires the explicit experimental FFI Winsock accept adapter plus private TCPWrap for connected sockets. Deno's direct Windows listener import still returns UV_EINVAL and is saved as a separate negative case. Its Windows Rust compatibility path wraps a listener as TcpStream; uv_tcp_open_listener is Unix-only. Both working Windows FFI adapters explicitly set FIONBIO to handle accept races. Deno uses infinite WSAPoll on an FFI pool thread; Bun uses a dedicated worker thread in the same process. Both close listener/drain then exit, with no readiness timer. Five repeated Windows three-worker/drain runs passed; safe unloading inside a long-lived runtime is not established. No runtime compilation or Go dependency. Portable release ZIPs/checksums and preparation stay in this test directory/build; no global installs. Those original ExtraHandle fixtures cover HTTP/1.1 only; the later TestRuntimeLifecycle matrix below adds HTTP/2, autoscaling, proxy and WebSocket coverage. Unix sockets remain outside these JS fixtures. Local native test/vet and CGO-disabled amd64/arm64 builds pass for both OSes; selected Linux lifecycle/control tests pass the race detector. The original TestProxyStack suite covers Python/Node/PHP; the common runtime matrix now extends proxy coverage; GitHub builds remain manual-only.

- The user explicitly requested a Socketify/ctypes workaround without native recompilation. TestSocketifyExtraHandle and tools/probes/socketify now preserve a positive Linux/Windows amd64 probe: three Python processes use Socketify's actual native HTTP/1.1 server on one extra inherited listener, read ordinary stdin, and exit successfully on interrupt while survivors continue accepting. OOTH_LISTEN_HANDLE is common to both OSes. The adapter creates a temporary loopback listener, stops its uSockets poll, pumps libuv to complete close, closes the temporary socket, reinitializes the poll with the inherited handle and restores uv_handle_t.data via ctypes. This depends on internal libuv-backed us_poll_t layout; SHA256 checks restrict it to the shipped binaries in Socketify commit d35e41921dadcd8e27679b7300b51e0a697bfcba. Test-only cffi/pycparser/setuptools and an extracted Linux libuv stay under ignored build/, with no application dependency, native compilation or toolchain patch. The original ExtraHandle fixture has no telemetry or draining assertions; its later protocol mode and TestRuntimeLifecycle now cover telemetry, autoscaling, active-request draining, Caddy/Nginx and native WebSockets. HTTP/2, TLS and Unix sockets remain unverified for this adapter. Do not describe it as a public adoption API. The later explicit env-handoff decision above changes the supervisor default without promoting this adapter to production stability. An upstream native API remains preferable to private memory-layout access.

- PHP TrueAsync was explicitly requested as another experimental worker runtime, using its built-in HTTP/1+2 server and one thread with coroutines. Official portable 0.10.0 PHP 8.6 binaries are tested under ignored build/runtime-trueasync. TestTrueAsyncCompatibility preserves the original audit: native HTTP/1.1 and h2c control listeners work on both OSes; Linux low-level stdin adoption works via fopen('php://fd/0') duplication (direct fd 0 hits a reactor bug); Windows CLI fails before script execution with a listening stdin. The Windows negative probe intentionally passes when reproducing that failure. TestTrueAsyncExtraHandle now verifies three workers sharing one listener while ordinary stdin remains usable: Linux uses Cmd.ExtraFiles fd 3 and async socket_accept; Windows uses DuplicateHandle/AdditionalInheritedHandles and a private FFI hook into exported zend_async_socket_listen_fd_fn, serving actual inherited-listener HTTP/1.1 and h2c with no PHP recompilation. The hook restores the pointer immediately, uses one server thread, and replaces the duplicate of an internally created temporary loopback listener (the unused original remains server-owned). This is a pinned-runtime diagnostic, not a stable adapter. Linux's official binary lacks FFI; its built-in server adoption remains unverified. A public adoption API remains missing; the later Windows protocol worker now covers telemetry, graceful draining and full ooth lifecycle. Linux remains limited by the released binary without FFI; no upstream issue/PR sent. The initial additional-handle probe established the convention: OOTH_LISTEN_HANDLE carries fd 3 on Linux or the native numeric handle on Windows, never a promised Windows CRT fd 3/4. Existing Go APIs suffice, no new toolchain patch/dependency. The later explicit user decision above makes env handoff the supervisor default; the original extra-handle diagnostic still lacks the protocol, while worker.php now implements it on Windows.

- Keep the supervisor in root `main.go`, approximately 1500 lines (a guideline, not a hard cap). The user explicitly approved small platform utility files: `job_windows.go` and `job_linux.go`, selected by Go's file suffixes. Their common API covers Start/Wait/Close and per-worker Kill/Close/Members. Do not add toolchain wrappers merely to avoid these files. Tests, examples and the toolchain patcher are separate.
- The user explicitly approved `github.com/sgtdi/fswatcher`, `github.com/goccy/go-yaml v1.19.2`, and `github.com/shirou/gopsutil/v4 v4.26.8`, including their Go dependencies. gopsutil is limited to CPU/memory calls audited to avoid subprocesses on Linux/Windows; do not claim every API on every platform avoids commands. No other application dependency is approved.
- Pool scaling now uses min_workers, max_workers, concurrency, scale_at (default 80%), scale_window (default 1s), and idle_timeout. Integrate capped request occupancy at every start/end transition; evaluate consecutive full windows, one extra worker per qualifying window. Reset for changed capacity or any unready/non-telemetry member. scale_delay remains a legacy alias for scale_window, with the new mean-occupancy behavior. This estimates utilization, not queue delay. Main resources defaults max_cpu_percent:90 and min_available_memory_percent:10 gate only optional growth; zero disables each threshold. Initial/invalid/stale (>3s) samples defer extras, never cold activation/minimum/startup/dependencies. One background gopsutil sampler per supervisor, approximately 1s; no per-worker sampler or new toolchain patch. Linux includes visible cgroup v2 ancestors of self and worker root, quota/cpuset usage and memory.max/current, including siblings. Hidden ancestors, v1 and Windows Job/container resource limits are not covered. Fixtures that measure worker policy alone explicitly disable resource gating. The pressure CSVs remain historical results of commit 40807dd's earlier policy, not measurements of this replacement.
- Build with `CGO_ENABLED=0`. Target Linux and Windows, TCP and Unix sockets. Runtime supervision must not launch management shell commands.
- The parent MUST NOT accept, proxy or copy application traffic. It owns and waits on a listener, passes it as an extra inherited handle by default (or explicit legacy stdin) to foreground workers, and receives telemetry or ordinary process output on stdout. Protocol workers send logs to stderr.
- Worker protocol is line-based ASCII `key=value`, version 1: `ready`, `start` and `end`; Unix-nanosecond timestamps, request IDs and nanosecond durations. No JSON. The user subsequently authorized a stdin stop command after a valid ready handshake; no separate control socket is required.
- Stdout telemetry is detected per worker from its first complete line: a valid version-1 ready event enables request accounting; anything else permanently selects ordinary stdout forwarding to stderr. Silent workers keep running without telemetry. Only telemetry enables request-based growth, socket idle shrinking and request watchdogs. Default readiness is started for all apps; explicit ready:event requires the handshake, while endpoint readiness remains a separate gate. Virtual dependency retirement still follows graph demand without telemetry. Tests cover silent/plain/overlong/partial first lines, later protocol-looking logs, and continued protocol-based lifecycle scaling.
- Application identity is configured with inline `user`/`group` on Linux and `user`/`password` on Windows. The user explicitly chose a password in YAML; do not replace it with a mandatory environment or secret-provider convention. Identity.apply stays in the platform utility files, with no additional toolchain patch. Linux uses existing Credential UID/GID/supplementary groups and static os/user lookup; Windows uses LogonUserW batch logon plus existing SysProcAttr.Token/CreateProcessAsUser. Native rights are required, with no fallback to the supervisor's identity; cgroup downgrade preserves credentials. No automatic profile/login environment or privilege grants. Do not expose password text in logs, argv or environment; YAML errors omit source excerpts. Linux different-user inherited-listener tests passed; Windows current account and failed authentication passed, but successful different-account Windows spawn remains unverified without suitable credentials/privileges. Optional test-only OOTH_TEST_WINDOWS_USER/PASSWORD enable it.
- The same configured identity owns filesystem Unix activation sockets: Linux chown UID/GID and configurable socket_mode (default 0660, octal or symbolic u/g/o/a =/+/- rwx clauses applied from 0660, Linux Unix sockets only), Windows owner SID plus protected ACL for that SID, the supervisor and SYSTEM. No identity means the supervisor account/group. TCP has no filesystem owner/mode. Permissions also update on listener reuse and roll back when snapshot application fails; failures reject configuration. Native Windows ownership privileges are required for a different owner, without automatic privilege escalation. Socket directories must protect the creation interval and allow intended client traversal. Windows operates on the AF_UNIX reparse point via OPEN_REPARSE_POINT, GetSecurityInfo/SetSecurityInfo, with no external command, dependency or toolchain addition. Local tests passed owner/ACL rollback and real inherited Unix-socket traffic on Windows; Linux passed different UID/GID with TCP and Unix. Runtime handle-adoption/accept support is still needed even though the worker does not create/bind the listener.
- Graceful shutdown uses the stdin stop command for workers with a valid ready handshake and a free stdin pipe, otherwise signals/OS notifications, followed by whole-worker-family termination after `stop_timeout`. Linux signal fallback uses SIGTERM; forceful shutdown uses cgroup.kill, or Process.Kill when cgroups are unavailable. Windows signal fallback uses WM_CLOSE for windows or CTRL_BREAK for a console group, including a hidden console when needed, then TerminateJobObject. Detached headless workers have no universal graceful operation. No Windows SCM integration is implemented.
- Dependencies use internal virtual activation: a validated DAG, lazy recursive demand, parallel independent branches, readiness gates, and reverse dependency shutdown. Programs without socket activation can use process-start or endpoint-probe readiness.
- User approved using the public `syscall.Epoll*` API directly on both platforms, extending it on Windows with AFD/IOCP. Keep this generic backend inside this repository's toolchain patch, separate from supervisor semantics. Current pinned compiler: Go 1.27.1, downloaded into `build/` by `build.sh`; never modify a global toolchain. When patch sources change, build.sh restores all affected stock source files from the verified archive before applying the new version.
- One goroutine and one epoll instance observe all listeners. Registrations use EPOLLIN|EPOLLONESHOT, unique IDs, and rearm on the manager's existing 25 ms tick after delivery. An internal loopback UDP wake socket terminates the blocking wait; it is not a worker control channel. There is no per-listener goroutine or WSAPoll loop. The Read(nil) wait and its Unix patch were removed.
- The Windows syscall extension supports sockets, ADD/MOD/DEL, level triggering, ONESHOT and IN/OUT/PRI/ERR/HUP/RDHUP events. EPOLLET and other unsupported flags are rejected. One owning AFD poller per underlying socket is an accepted user constraint; duplicated/inherited handles do not authorize additional registrations. DEL before closing/reusing a socket. One Wait caller; Ctl may run concurrently. EpollEvent Fd/Pad are opaque data, while EpollCtl takes the full-width handle. EpollClose cancels/drains native requests before unpinning buffers; Linux has the same close spelling as a small wrapper around Close.
- AFD uses a separate device handle and IOCP; it never binds the monitored sockets to its completion port. Implementation references are wepoll and libuv, with wepoll attribution in THIRD_PARTY_NOTICES.md. No Rust, C, CGO or new binary/library dependency was introduced. Do not claim full epoll compatibility or measured application-throughput improvements.
- Windows Read(nil) on an accepted connection and Read(nil) on a listening socket are different. Go issues 15735/27315 and CL 22031 do not establish that WSARecv on a listener waits for incoming connections. Stock Go 1.24.3/1.27.1 probes confirmed this locally. Windows Go workers sharing a listener need the patched toolchain as well.
- Audit confirmed standard Cmd.Stdin inheritance works: the former syscall.StartProcess socket-inheritance override was removed and TCP/Unix lifecycle tests pass. Listener initialization now tries normal IOCP first; only ERROR_INVALID_PARAMETER on a listening socket enables the existing Go event-based I/O path with patched deadline/close tracking. Stock Go 1.27.1 handoff probes show the first worker accepts, then the second fails net.FileListener, even after killing/waiting for the first. Do not claim stdin conversion itself fails or every individual stock Go worker fails. Three patched workers pass. Only the inherited AcceptEx fallback still checks deadline/close every 20 ms; distinguish that worker-side behavior from the supervisor's asynchronous AFD poller. Stock Process.Kill works; stock Windows Process.Signal(os.Interrupt) remains unsupported. Cmd.NewProcessGroup wraps existing SysProcAttr.CreationFlags plus hidden-console setup to keep main.go portable.
- PHP's TCP FastCGI source retrieves a native socket with _get_osfhandle and uses accept; this is handle retrieval, not IOCP reassociation. That initial PHP audit inspected source; the later proxy suite also executed PHP. libuv already falls back to local completion events for imported sockets. On Node 24.19.0/libuv 1.52.1, three workers passed using a diagnostic private TCPWrap binding and an extra inherited native handle; public listen({fd: 0}) and listen(process.stdin) failed as expected from Node's Windows limitation. The later user decision explicitly adopts extra handle/environment as the default convention; the private Node binding remains an experimental runtime adapter. Reproduction is in tools/probes.
- Node 26.10.0/libuv 1.52.1 subsequently passed with stdin alone: built-in node:ffi calls GetStdHandle or _get_osfhandle(0), then private TCPWrap adopts the socket. Both probes served requests from three workers with a stock Go parent, no extra handle/environment, no npm dependency or Node patch. This remains an experimental/private API fixture under tools/probes, not a supported production adapter. OOTH_TEST_NODE enables TestNodeWorker: cold activation, idle replacement and active-request draining with a graceful-exit marker. Linux uses the public fd API. The current Node worker reads OOTH_LISTEN_HANDLE and needs only the private TCPWrap binding on Windows; FFI is required only for its legacy stdin path. Portable test runtimes stay under ignored build/, verified against official archive checksums; do not install globally or add them as ooth dependencies.
- epoll_test.go checks traffic remains unconsumed, level/oneshot behavior, pending MOD/DEL/re-add, concurrent registration, cancel/drain on close, OUT/RDHUP, and 128 idle listeners without per-socket goroutine growth. BenchmarkEpollIdleListeners measures nonblocking wait overhead, not end-to-end throughput.
- Linux uses SIGCHLD to reap adopted children through `/proc/self/task/*/children`, skipping registered direct workers under the same lock used during Start. PID 1 uses automatic adoption; every other PID enables subreaper independently of cgroup availability. A blocked prctl produces a separate warning, while direct-child waits and available cgroups continue. Restore the previous subreaper state when closing the owner; the previous periodic PID 1 scan was removed. Subreaping and cgroup membership are separate concerns. Cmd.Wait still observes each direct child's exit; final telemetry is drained with a 100 ms bound on a retained stdout pipe.
- Descendant ownership is now maintained by the kernel from process creation: one Windows Job or Linux cgroup v2 per worker instance. A foreground worker exit ends its instance; remaining descendants are terminated and cleanup completes before restart. Windows Jobs disallow breakaway and use KILL_ON_JOB_CLOSE; the only new spawn patch adds SysProcAttr.JobObjects and PROC_THREAD_ATTRIBUTE_JOB_LIST. All Job utility calls stay outside the stdlib in job_windows.go. Linux uses existing UseCgroupFD/CgroupFD, cgroup.kill and event-driven empty-group waits; no added Linux toolchain API is needed. Kernel membership survives an intermediate parent exiting immediately; no periodic ancestry scan is used.
- The user explicitly chose to keep running when Linux cgroup supervision is unavailable, with a stderr warning and direct-child supervision only. Optional main-config cgroup selects the delegated root (restart required to change); otherwise use OOTH_CGROUP_ROOT then the current cgroup under /sys/fs/cgroup. Check cgroup.kill availability; if placement fails but the same command starts without placement, warn and disable placement for subsequent workers. Root is not mandatory: writable delegation and migration permissions suffice. Both PID 1 reaping and non-PID-1 subreaping remain enabled without cgroups; adoption alone does not recover the original service membership. Do not silently claim full tree ownership in fallback mode.
- The common API does not promise events for every descendant birth/exit. Subreaper alone does not expose those events and Windows Job completion messages are not guaranteed; membership is authoritative. Linux cannot automatically kill members merely when ooth's cgroup handle closes; recovery after ooth itself is forcibly killed is not implemented. The groups supervise cooperative workers, not hostile code escaping through privileged migration or unrelated system services.
- Local Job/cgroup tests passed immediate ancestor exit, remaining-descendant cleanup, crash restart, Windows denied breakaway and cleanup when the Job owner exits. Linux was tested as a non-root user inside a delegated cgroup, with missing delegation and with migration denied from outside a writable delegation. Go 1.27 Cmd.Start cannot be retried on the same Cmd after failure: the placement fallback creates a fresh Cmd using ooth's launch parameters; Start returns the active command in processJob, and Wait receives that job. The workflow prepares a Linux delegation for these tests but remains manual-only.
- Root `src/` and `golang_patch/` retain the historical prototype and patch as separate modules. They are not the current build target.
- Run `bash ./build.sh` on Linux or Git Bash on Windows. It runs native tests/vet and emits amd64/arm64 binaries for the host OS. `OOTH_TEST_PYTHON` enables external-language integration tests. Windows CPython is tested with TCP; its AF_UNIX accept path is unavailable. ARM64 is cross-compiled, not runtime-tested here.
- Development validation is local. Do not run GitHub builds or publish releases for every change, and do not add push/PR/scheduled CI triggers. The user wants manual GitHub verification at agreed milestones; do not automatically dispatch the workflow after ordinary commits. The workflow is workflow_dispatch-only and tests both OSes. Its publish_release boolean defaults to false; enable it only when publishing a milestone prerelease with binaries and SHA256SUMS is intended. README describes configuration, protocol, patch requirements and limitations. Repository: https://github.com/mefistofelix/ooth (public, as explicitly requested by the user).
- Local validation also passed the Linux race detector, actual namespace PID 1 orphan reaping, graceful/forceful shutdown without a Windows console or inherited standard handles, and a WinForms FormClosing handler invoked by WM_CLOSE. Reproducible manual probes are under `tools/probes`. Only visible GUI windows are sent WM_CLOSE: framework-internal hidden windows must not receive it.
- The user requested committed, reproducible end-to-end suites using both Caddy and Nginx started by ooth init. TestProxyStack and tools/probes/proxy store all config templates, PHP fixture and preparation scripts; downloaded runtimes and rendered configs/logs stay under build/. Verified Caddy 2.11.4 and Nginx 1.30.5 with Python HTTP/1.1, Node 26.10.0 h2c, and stock PHP-CGI on Linux/Windows. Node/Python verify cold activation, two serving PIDs, balanced start/end events, idle zero and reactivation. PHP has no handshake and remains one ordinary worker beyond the timeouts. Windows PHP's native detection requires INVALID_HANDLE_VALUE for stdout/stderr: the explicit standalone php_worker_windows.go launcher supplies that convention, preserves inherited stdin/Job/console group and waits for PHP, without proxying traffic or another toolchain patch. TestStockPHPWindowsInheritedDetection preserves the original incompatibility. Do not claim unwrapped Windows PHP works with normal stdout/stderr or guaranteed graceful PHP request draining on Windows.
- HTTP/2 connections pin streams to their accepting worker. The experimental h2c fixture rotates sessions with GOAWAY to exercise new connections after process growth; no throughput/production pooling claims. Request start debug logging complements existing finish logs. Fixed idle-zero regression: draining workers retain dependencies but must not create replacement demand for their own service. TestActivationLifecycle now explicitly asserts zero live workers before reactivation. tools/probes/with-cgroup.sh preserves the prior local delegation/race/fallback setup as a portable test helper; runtime ooth never invokes it.
- Pressure research is opt-in through OOTH_TEST_PRESSURE, TestPressure and TestPressureProxies. Fixtures, analyzer, methodology, findings and portable aggregate CSVs are in tools/probes/pressure; generated configs and raw traces stay in ignored build/. Local validation completed 61 scenarios twice on Windows and twice on Linux/WSL2 (244 case executions, 26,560 successful requests), using Go/Python/Node/PHP and Caddy/Nginx spawned through ooth. Arrival pacing is independent of responses; delays vary deterministically. Unlike the earlier lifecycle fixture, the pressure h2c worker retains sessions without forced GOAWAY. Debug listener events carry EpollWait-return observation timestamps. Accept timestamps travel only in test response bodies; the production stdout protocol and scaling policy are unchanged. Notification rate/persistence is not a universal pressure metric, and next-accept pairing can select the wrong connection. Internal application queues and existing h2c sessions can accumulate waiting without listener notifications; more workers only help if future work reaches them. See RESULTS.md before proposing an autoscaling heuristic or claiming measurements of exact kernel queue times. No new application dependency or toolchain patch was added.
